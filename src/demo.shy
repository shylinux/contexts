# demo script

cmd space

let a = 1;

let a, b = 1, 2

if a > 1 { pwd }

if a, b = 2, 4; a > 1 { pwd }

for a = 2; a < 5; a++ { pwd }

func show(a, b) (c, d) {
	let c = system(ls, a)
	return
	pwd
}
let e, f = show(etc, bin)
